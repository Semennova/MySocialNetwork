import React from 'react'
import loading from '../../assets/img/loading.gif'

let Loader = (props) => {
    return (
        <div>
            <img src={loading}/>
        </div>
    )
}

export default Loader