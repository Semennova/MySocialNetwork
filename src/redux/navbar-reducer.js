let initialState = {
    
    navbar: [
        {id: 1, name: 'Anka'}, 
        {id: 2, name: 'MicStuck'},
        {id: 3, name: 'Gabster'}
    ]
}

const navBarReducer = (state = initialState) => {


    return state;
}

export default navBarReducer;